flask>=2.2
flask-cors>=3.0
llama-index>=0.9.0
google-genai>=0.5.0
transformers>=4.30.0
torch>=2.0.0
faiss-cpu>=1.7.4
PyMuPDF>=1.22.0
pandas>=2.0.0
huggingface-hub>=0.16.0
sentencepiece>=0.1.98
python-dotenv>=1.0.0

# Optional / useful (uncomment if needed)
# - For faster embeddings or model serving you might need `accelerate` and `bitsandbytes`.
# accelerate>=0.20.3
# bitsandbytes>=0.40.0
